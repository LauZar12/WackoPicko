namespace: wackopicko-sast
working_dir: /app
language: EN

output:
  file_path: /app/sast-report.csv
  format: CSV

sast:
  include:
    - .
  exclude:
    - glob(**/upload/**)
    - glob(**/node_modules/**)
    - glob(**/vendor/**)
